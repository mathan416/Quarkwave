# Documentation gaps and verification queue

**Status:** source-reviewed, partial browser hardware check (firmware snapshot: 2026-09-23). The earlier explicit-sync failure was reproduced and resolved on the assembled boards; other entries still need hardware verification. Resolve or confirm them before turning the development guides into a public manual.

| Priority | Finding and source | Documentation consequence / next check |
| --- | --- | --- |
| High | With Uno Wi-Fi and RTP disabled, the same four-voice, three-unison, all-effects benchmark completed **110,251 DAC writes in five seconds**: the requested 22,050/s average. A timing-instrumented run had no dropped catch-up windows, but 2,381 writes began over 100 µs late and the worst recorded lateness was 1,734 µs. Disabling matrix redraws made only a small difference, so that change was rejected. [Uno audio loop](../QuarkWave_MIDI/QuarkWave_MIDI.ino), [current test record](hardware-test-log.md#uno-network-free-performance-and-wireless-disconnect--2026-09-23). | Measure physical DAC intervals and audible output under sustained four-voice play and MIDI traffic. Check RTP note latency, 40/120/240 BPM clock and transport through the Pico. The output circuit is unbuilt, so the sample counter is not a measured audio waveform or proof of evenly spaced 22.05 kHz output. |
| High | In the optional combined USB MIDI/audio image, a clean steady interval made **154,337 DAC writes in 7,000 USB audio packets**, or **22,048.1/s**, with no new underrun, overrun, or scheduler slip. The four-voice, three-unison, all-effects test made **319,824 writes in 15,000 packets**, or **21,321.6/s**, with **10,959 USB audio underruns** and **240 slips**. The host still received 441,344 samples in 20.02 seconds because missing ring samples were filled with zero. [USB audio diagnostic](../experiments/usb-audio/UsbAudioCapture.h), [test record](hardware-test-log.md#usb-midi-audio-regression-suite--2026-09-23). | Optimize the combined audio workload or define a supported effect/voice limit, then repeat the same stress and listening tests. Do not describe the composite build as sustaining 22.05 kHz under all effects. The physical DAC timing and audible effect of inserted zero samples are unmeasured. |
| Medium | The reproduced explicit-sync timeout was resolved by parsing the Uno's fixed six-byte status replies directly on the Pico. The automated release-build sequence stayed connected through explicit sync, Randomize, and patch loads. [Pico link parser](../QuarkWave_UI_MIDI/QuarkWave_UI_MIDI.ino), [Uno receive loop](../QuarkWave_MIDI/QuarkWave_MIDI.ino), [test record](hardware-test-log.md#return-link-parser-fix--2026-09-23). | Remaining acceptance: measure the Pico RX high voltage, disconnect the return wire to check offline detection, force a lost acknowledgment, and test both boards starting together and either board starting late. |
| High | Standalone activity is tracked when DIN or optional USB delivers Note On/Off, CC, pitch bend, or an accepted QuarkWave sound command, or when the Pico marks forwarded RTP sound activity; MIDI clock and transport are excluded. No physical-controller-presence detection exists. A live USB MIDI note set `mode: standalone` while the Pico link stayed connected. [Uno external handlers](../QuarkWave_MIDI/QuarkWave_MIDI.ino), [USB receiver](../experiments/usb-audio/UsbMidiInput.h), [Pico auto-sync rule](../QuarkWave_UI_MIDI/QuarkWave_UI_MIDI.ino), [test record](hardware-test-log.md#composite-uno-usb-midi-and-audio--2026-09-23). | Run the Uno without the Pico, attach a controller, change the sound, then attach the Pico. Confirm the sound persists until explicit **Sync Pico patch to Uno**, and that the warning precedes the reset. |
| Medium | The optional composite Uno image presented a Mac USB MIDI port and 22,050 Hz mono audio input; a USB MIDI Middle C produced recorded audio during a short test and a 15-second held-note test while the Pico link stayed connected. A later four-voice/all-effects capture found the rate shortfall above. The USB disconnect release and DIN → USB → RTP clock priority are source and packet tested, not live accepted. [USB experiment](../experiments/usb-audio/README.md), [USB receiver](../experiments/usb-audio/UsbMidiInput.h), [test record](hardware-test-log.md#usb-midi-audio-regression-suite--2026-09-23). | Try the combined image in Logic Pro, exercise USB note/pedal release on suspend and disconnect while the Uno stays powered, and measure clock/transport timing and source failover. Check Windows enumeration separately. |
| Medium | Fresh user slots are now empty, while any existing file—including a legacy `P0`…`P7` file or unreadable file—is preserved as occupied. Save As requires an explicit destination and confirmation for replacement. [Pico storage and save handler](../QuarkWave_UI_MIDI/QuarkWave_UI_MIDI.ino), [Save As dialog](../QuarkWave_UI_MIDI/QuarkWave_UI.h). | On hardware, check blank storage, upgrades with legacy files, full slots, unreadable files, write failures, and reboot after loading a factory or user patch. Confirm save-result messages and displayed selection. |
| Medium | The redesigned panel wires its visible buttons to the existing `type` messages; Randomize sends one `randomize` request to the Pico and returns `patchData`. Browser rendering and control messages were checked in headless Chrome; per-control Uno MIDI reception and audio remain unverified. The new VU drawing is source-reviewed and its selection command reached the live Uno, but the moving bars have not been visually accepted on the board. [Browser handlers and view switching](../QuarkWave_UI_MIDI/QuarkWave_UI.h), [Pico dispatch](../QuarkWave_UI_MIDI/QuarkWave_UI_MIDI.ino), [Uno VU display](../QuarkWave_MIDI/QuarkWave_MIDI.ino). | Browser checks passed for 50 control paths, 41 pointer keys, view switching with a held browser note, Randomize, Load, Save As dialog, rejected saves, visualization messages, and Panic message. Test the VU bars during quiet, sustained, and percussive notes; then test Uno response and audio, successful Save/Save As, Commit/Load Commit, and repeat view/keyboard checks after link recovery. |
| Medium | The Uno top row now has Pico sound, external activity, clock, sustain, arpeggiator, voice-steal, and audio-scheduling indications in addition to the existing link, receive, note, and heartbeat pixels. The source ran during USB-audio tests: the Pico link stayed connected and the owner confirmed the matrix responds to notes, but the new status pixels have not been individually accepted. [Uno LED code](../QuarkWave_MIDI/QuarkWave_MIDI.ino), [LED guide](led-display-guide.md), [test record](hardware-test-log.md#uno-top-row-status-indicators--2026-09-23), [USB-audio experiment](../experiments/usb-audio/README.md). | Observe the new pixels on the assembled boards, especially five overlapping notes for voice stealing and a temporary forced-delay diagnostic for column 11. Verify the note flash in Status, VU, and Scope. |
| Medium | DIN and the Pico RTP gateway now accept sound Program Changes and QuarkWave SysEx 01–03, while visualization, reset, and link commands remain Pico-only. Each input has its own clock history, with DIN priority and transport-controlled arpeggiator scheduling. [Uno input handlers](../QuarkWave_MIDI/QuarkWave_MIDI.ino), [Pico gateway](../QuarkWave_UI_MIDI/QuarkWave_UI_MIDI.ino). | On hardware, test accepted and rejected commands, 40/120/240 BPM clock, beat alignment, Start/Continue/Stop, DIN priority, two-second failover, and held tempo after clock loss. |
| Medium | The Pico now permits one RTP session, tracks its held notes and sustain pedal, and releases both when AppleMIDI reports disconnection. The Uno distinguishes browser, DIN, and wireless ownership. Live RTP disconnect diagnostics confirmed note release, preservation of the same browser-held pitch, wireless pedal release, and preservation of a browser-held pedal. [Pico release](../QuarkWave_UI_MIDI/QuarkWave_UI_MIDI.ino), [Uno ownership](../QuarkWave_MIDI/QuarkWave_MIDI.ino), [test record](hardware-test-log.md#uno-network-free-performance-and-wireless-disconnect--2026-09-23). | Test DIN overlap after its physical port is built, long silent network loss until AppleMIDI timeout, and multiple-channel RTP note overlap. Use browser **Panic** if an immediate release is needed before timeout. |
| Medium | The [Mac and Windows network MIDI setup](network-midi-setup.md) follows the Pico's session and port plus Apple and rtpMIDI setup instructions. A direct RTP invitation and note passed a two-board smoke check, but the Mac/Windows procedures, Logic-to-Pico MIDI sequencing, Windows 11 compatibility, and Logic external-clock timing have not been accepted on the assembled instrument. The owner confirmed Logic with the separate USB-audio input without specifying whether RTP was used. [Pico session](../QuarkWave_UI_MIDI/QuarkWave_UI_MIDI.ino), [USB-audio result](hardware-test-log.md#logic-pro-usb-audio-owner-check--2026-09-23). | Walk through both platform procedures, confirm a note and note release from each DAW, record MIDI and USB audio in Logic, then test tempo/transport and disconnect. Record the exact Windows version and MIDI driver version. |
| Medium | The [owner's photographs](connection-guide.md#the-current-setup) show the two USB-powered boards and level-shifter module; the owner confirms the return-wire shift and common ground. Both UART signal wires appear to use separate level-shifter channels; exact channel pairs, LV/HV supply terminals, and Pico RX voltage are unmeasured. The Uno's `A0` audio stage is not built. The owner also has a [TDA1308 headphone board](connection-guide.md#add-headphones-with-the-tda1308-board), not yet connected; DIN hardware is not shown. [Pico wiring comment](../QuarkWave_UI_MIDI/QuarkWave_UI_MIDI.ino), [Uno MIDI instance](../QuarkWave_MIDI/QuarkWave_MIDI.ino), [DAC write](../QuarkWave_MIDI/QuarkWave_MIDI.ino). | Use the [current inter-board schematic](connection-guide.md#the-current-setup) to continuity-check both UART paths, supply terminals, and common ground; measure Pico RX. Build and test the proposed mono line and headphone outputs before treating their diagrams as as-built or quoting audio levels. Select, build, and document the [opto-isolated DIN input](connection-guide.md#add-a-wired-midi-input), then verify its isolation and MIDI reception. |

| Medium | Opening the illustrated Connection guide coincided with a Pico HTTP stall while its USB serial loop stayed alive. A restart restored quick responses; a later 45-request stress check had one truncated JPEG and no full stall. Help fetch timeout and one image retry are deployed, but the underlying intermittent network/serving cause is not isolated. [Help serving check](hardware-test-log.md#onboard-help-serving-check--2026-09-23), [Help loader](../QuarkWave_UI_MIDI/QuarkWave_UI.h). | Revisit after extended Pico uptime with the normal browser open; record uptime, free heap, RSSI, browser request failures, and whether WebSocket MIDI stays responsive. If the stall recurs, capture serial and network timing before rebooting. |

Use the [hardware test log](hardware-test-log.md) to record observations for each assembled revision.

The browser and full illustrated Help were uploaded to the Pico on 2026-09-23. A live check showed **Pico: Connected**, **Uno: Connected to Pico**, the preserved user patch, and working guide and LED-image routes. The explicit-sync link failure was fixed and retested earlier. The physical A0 output and per-command audible responses still need their own checks.

## Hands-on acceptance pass, when both boards are available

1. Record board/core and library versions, the three source hashes from the [index](README.md), and the assembled wiring revision.
2. Record the owner-confirmed return-wire level shift, continuity-checked jumper endpoints, and measured Pico RX voltage, then power both boards via their USB ports; check Wi-Fi, `quarkwave.local`, IP fallback, separate **Pico** and **Uno** connection labels, note playback once the audio stage exists, and note release on browser disconnect.
3. Compare browser controls with Uno behavior for each group: envelope, tone, modulation, arpeggiator, delay, chorus, bitcrush, tremolo, drive/fold, and Panic. Log MIDI CC values and compare panel readouts, including vibrato rate and master volume; repeat Randomize and check its full-range effects. Check keyboard velocity 1, 100, and 127 and view switching with a held note.
4. Save and load a user patch, factory preset, factory-to-user copy, and Commit snapshot; restart each board separately and confirm the selected Pico patch is restored on clean Uno startup without opening a browser. Test a lost completion acknowledgment and return-wire disconnection.
5. Exercise DIN notes, bend, CC, and clock without the Pico; then test the Pico RTP gateway with notes, bend, CC, and clock. Confirm standalone activity blocks automatic takeover, while clock alone does not. Check the documented Program Change and SysEx allowlist on each input, including malformed messages and standalone status. Check that BLE is absent in this build.
6. Update the guides with verified results, then remove resolved entries from this queue or link them to development issues.
